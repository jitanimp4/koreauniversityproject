#pragma once
// forgers_table.h
// Exposes The Forger's Table minigame as a single callable function.
// Returns true if the player completed a full round, false if they quit early (Q).
bool runForgersTable();
